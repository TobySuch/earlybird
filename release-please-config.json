{
  "release-type": "python",
  "pull-request-title-pattern": "chore: release ${version}",
  "packages": {
    ".": {
      "extra-files": [
        {
          "path": "uv.lock",
          "type": "toml",
          "jsonpath": "$.package[?(@.name=='earlybird')].version"
        }
      ]
    }
  }
}
