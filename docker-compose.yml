services:
  earlybird:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # SQLite database, Gmail OAuth token, and audio files — persisted outside the container
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/earlybird.db
    env_file:
      - .env
